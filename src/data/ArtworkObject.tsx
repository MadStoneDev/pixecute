import { Artwork, ArtworkObject } from "@/types/canvas";
import { NewArtLayer } from "@/data/ArtLayer";

export const NewArtworkObject: Artwork = {
  layers: [NewArtLayer],
  frames: [100],
  keyIdentifier: "",
};
